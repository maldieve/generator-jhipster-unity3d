#if UNITY_5_3_OR_NEWER
#define NOESIS
using Noesis;
#else
using System.Windows.Controls;
#endif

using System.Windows.Input;
using System.Collections.Generic;

namespace <%= unity3dAppName %>.View.Entities
{
  [Serializable]
  public partial class Base<%= persistClass %>View : UserControl
  {
    public TemplateQuizzView()
    {
        InitializeComponent();
    }

#if NOESIS

    private void InitializeComponent()
    {
        DataContext = new <%= persistClass %>ViewModel();
        NoesisUnity.LoadComponent(this);
    }
#endif

  }
}
