#if UNITY_5_3_OR_NEWER
#define NOESIS
using Noesis;
using System.Collections.Generic;
#else
using System.Windows.Controls;
#endif

using System.Windows.Input;
using System.Collections.Generic;

namespace <%= unity3dAppName %>
{
  public partial class <%= persistClass %>View : UserControl
  {
   object param = null;

   public Base<%= persistClass %>View()
    {
      InitializeComponent();
    }

    public Base<%= persistClass %>View(object param)
    {
      this.param = param;
      InitializeComponent();
    }

#if NOESIS

    private void InitializeComponent()
    {
      DataContext = new <%= persistClass %>ViewModel(param);
      NoesisUnity.LoadComponent(this);
    }
#endif

  }
}
