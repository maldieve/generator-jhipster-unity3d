#if UNITY_5_3_OR_NEWER
#define NOESIS
using Noesis;
using System.Collections.Generic;
#else
using System.Windows.Controls;
#endif

using System.Windows.Input;
using System.Collections.Generic;

namespace <%= unity3dAppName %>
{
  public partial class <%= persistClass %>DetailView : UserControl
  {
   object param = null;

   public <%= persistClass %>DetailView()
    {
      InitializeComponent();
    }

    public <%= persistClass %>DetailView(object param)
    {
      this.param = param;
      InitializeComponent();
    }

#if NOESIS

    private void InitializeComponent()
    {
      DataContext = new <%= persistClass %>DetailViewModel(param);
      NoesisUnity.LoadComponent(this);
    }
#endif

  }
}
